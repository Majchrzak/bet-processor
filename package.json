{
  "name": "bet-processor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "benchmark": "pnpm --filter @bet-processor/api build && pnpm --filter @bet-processor/tools build && node packages/tools/dist/main.js benchmark",
    "docker:down": "docker compose down",
    "docker:up": "docker compose up --build --wait",
    "lint": "pnpm -r lint",
    "run-game": "pnpm --filter @bet-processor/api build && pnpm --filter @bet-processor/tools build && node packages/tools/dist/main.js run",
    "seed": "pnpm --filter @bet-processor/api build && pnpm --filter @bet-processor/tools build && node packages/tools/dist/main.js seed",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/node": "^24.3.0",
    "eslint": "^9.34.0",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0",
    "vitest": "^3.2.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
